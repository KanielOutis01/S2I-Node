#!/bin/bash
set -e

echo "---> Copy source code"
cp -R /tmp/src/. ./

echo "---> Install dependencies"
npm install

echo "---> Skipping Angular build (dev mode)"

echo "---> Fix permissions"
fix-permissions /opt/app-root